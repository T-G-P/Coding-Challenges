server {
    listen       8080;
    server_name  localhost;

    charset utf-8;

    location / { try_files $uri @chartbeat_app; }

    location @chartbeat_app{
        include uwsgi_params;
        uwsgi_pass unix:/chartbeat_app_uwsgi.sock;
    }

}
